# Нерешённые развилки

Не блокируют MVP, но держать в фокусе. Реши — обнови этот файл и, если нужно, `architecture/overview.md`.

1. **Режим по умолчанию для первой демо.** Уже зафиксировано: AI выключен, «Только правила» (см. `overview.md`). Пересмотреть после первого показа? Стоимость Claude API при разовых сканах минимальна.
2. **E-commerce-правила (v1.5).** Делать сразу после MVP или после первой обратной связи по базовому cookie-сканеру?
3. **Публичный «значок скана».** Юридически рискованная фича (близко к анти-паттерну accessiBe) — отдельный разбор до реализации.
